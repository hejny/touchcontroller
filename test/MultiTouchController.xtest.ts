/*
import TouchController from "../src/TouchController";
import MultiTouchController from "../src/MultiTouchController";

describe("MultiTouchController", function() {

    const touchController = new TouchController(new HTMLElement());
    const multiTouchController = new MultiTouchController(
        touchController,
        (position)=>position.x
        );


    touchController.touchStart('touch0','TOUCH',{clientX:0,clientY:0});


    it("should ", function() {
        expect()
    });








});


*/
